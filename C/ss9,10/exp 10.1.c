#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int num;
	printf("Cac so chan tu 1 den 30 la : \n");
	for(num = 2; num <= 30; num+=2)
	{
	printf("\n%d",num);
	}
	return 0;
}
